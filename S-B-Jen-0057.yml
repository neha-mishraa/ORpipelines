resources:
  - name: valueStore
    type: PropertyBag
    configuration:
      key: value
pipelines:
  - name: testJenkinsOR
    steps:
      - name: bashStep
        type: Bash 
        configuration:
          inputResources:
            - name: valueStore
        execution:
          onStart:
            - echo "executing onStart"
      - name: jenkinsStep
        type: Jenkins
        configuration:
          outputResources:
            - name: valueStore
          jenkinsJobName: testPipelineOR
          integrations:
            - name: jenkins
