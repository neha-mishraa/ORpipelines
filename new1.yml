pipelines:
  - name: testpipeLine
    steps:
      - name: testStep
        type: Bash
        execution:
          onStart:
            - echo "executing onStart"
          onExecute:
            - echo "executing onExecute"
